<div class="page-header">
  <h3>Location #<%= @location.id %></h3>
</div>

<div class="row">
  <div class="col-md-12 mb-2">
    <dl class="dl-horizontal">
      <dt>Company</dt>
      <dd><%= Company.find(@location.company_id).name %></dd>

      <dt>City</dt>
      <dd><%= @location.city %></dd>

      <dt>State</dt>
      <dd><%= @location.state %></dd>

      
        <dt>Products Shipped</dt>
          <dd><table class="table">  
            <% Locationdetail.where(location_id: @location.id).each do |location| %>
            <li><%= Product.find(location.product_id).name %></a></li>
            <%end%>
        </dd>

    </dl>

    <div class="btn-group btn-group-justified">
      <a href="/locations" class="btn btn-primary">
        Back
      </a>
      <a href="/locations/<%= @location.id %>/edit" class="btn btn-warning">
        Edit
      </a>
      <a href="/delete_location/<%= @location.id %>" class="btn btn-danger" rel="nofollow">
        Delete
      </a>
    </div>
  </div>
</div>
