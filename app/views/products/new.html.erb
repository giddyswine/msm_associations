<!-- Validation failure messages -->
<% if @product.errors.any? %>
  <% @product.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Product</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_product" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for name -->
      <div class="form-group">
        <label for="name" class="control-label">
          Name
        </label>

        <input type="text" id="name" name="name" placeholder="Name" class="form-control" value="<%= @product.name %>">
      </div>

      <!-- Label and input for ncontent -->
      <div class="form-group">
        <label for="ncontent" class="control-label">
          Ncontent
        </label>

        <input type="text" id="ncontent" name="ncontent" placeholder="Ncontent" class="form-control" value="<%= @product.ncontent %>">
      </div>

      <button class="btn btn-success">
        Create Product
      </button>
      or
      <a href="/products">Cancel</a>
    </form>
  </div>
</div>
