<div class="page-header">
  <h1>
    Salespeople
    <a href="/salespeople/new" class="btn btn-lg btn-success">New Salesperson</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Salesperson</th>
        <th>Actions</th>
      </tr>

      <% @salespeople.each do |salesperson| %>
      <tr>
        <td><%= salesperson.salesperson %></td>
        <td>
          <a href="/salespeople/<%= salesperson.id %>" class="btn btn-primary">Show</a>
          <a href="/salespeople/<%= salesperson.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_salesperson/<%= salesperson.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
