<div class="page-header">
  <h1>
    Companies
    <a href="/companies/new" class="btn btn-sm btn-success">New Company</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Name</th>
        <th>Actions</th>
      </tr>

      <% @companies.each do |company| %>
      <tr>
        <td><%= company.name %></td>
        <td>
          <a href="/companies/<%= company.id %>" class="btn-xs btn-primary">Show</a>
          <a href="/companies/<%= company.id %>/edit" class="btn-xs btn-warning">Edit</a>
          <a href="/delete_company/<%= company.id %>" class="btn-xs btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
